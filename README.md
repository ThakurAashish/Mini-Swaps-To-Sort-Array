# Mini-Swaps-To-Sort-Array
This algorithm finds the minimum number of swaps required to sort an array in ascending order. It works by pairing each element with its original index, then sorting these pairs by value. After sorting, the array positions form cycles.
